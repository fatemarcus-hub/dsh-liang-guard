{
  "name": "dsh-liang-guard",
  "version": "1.2.0",
  "description": "梁神卫士 (Liang Guard) — a system-guard plugin for DeepSeek Harness (DSH) web UI: one-click health check, real Windows Defender scans, network speed test & repair, live system metrics with one-click cache cleanup. Injects a guarded button under the sidebar's New Session button; all APIs ride the DSH webserver's same-origin route.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": { "default": "./lib/index.js" },
    "./client": { "default": "./lib/client.js" },
    "./package.json": "./package.json"
  },
  "dsh": {
    "client": {
      "inject": [],
      "platform": "web"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fatemarcus-hub/dsh-liang-guard.git"
  },
  "keywords": [
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "windows-defender",
    "system-monitor",
    "liang-guard"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22"
  }
}
